name: flutter_web_diary
description: smiple fast secure.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^0.4.0+9  
  provider: ^4.0.4
  cloud_firestore: ^0.13.4
  slimy_card: ^1.0.4
  google_sign_in: ^4.5.1
  rxdart: ^0.21.0
  firebase_auth: ^0.16.1
  flutter_form_builder: ^3.0.0-beta.2
  cupertino_icons: ^0.1.2
  flutter_chips_input: ^1.9.0-dev.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
